apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

labels:
  - pairs:
      lornu.ai/cloud: gcp
      lornu.ai/cluster: lornu-gke-prod
    includeSelectors: false
