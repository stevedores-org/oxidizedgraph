# Azure AD Workload Identity — production value from crossplane-heaven#5 / Flux.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: oxidizedgraph
  annotations:
    azure.workload.identity/client-id: REPLACE_ME
