apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oxidizedgraph

resources:
  - ../../base

# AKS hub overlay — Azure AD Workload Identity (crossplane-heaven#5).
patches:
  - path: serviceaccount-wi.yaml
  - path: deployment-env.yaml

images:
  - name: ghcr.io/stevedores-org/oxidizedgraph/server
    newTag: "0.2.0"
