# GKE Workload Identity — production value from crossplane-heaven / platform ops.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: oxidizedgraph
  annotations:
    iam.gke.io/gcp-service-account: REPLACE_ME@PROJECT_ID.iam.gserviceaccount.com
