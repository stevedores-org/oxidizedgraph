# Env-specific: set your GCP SA before apply (overlay only — never commit a real project email to base).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: oxidizedgraph
  annotations:
    iam.gke.io/gcp-service-account: REPLACE_ME@PROJECT_ID.iam.gserviceaccount.com
